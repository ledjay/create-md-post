export type postDataType = {
  title: string;
  date: string;
  description: string;
};

export type lingoStringsTypes = { [key: string]: string[] };

export type configFileType = {
  path: string;
};
